schemaVersion: 1.0
steps:
  - task: powershell
    description: "Install required software for Analytics Persona"
    script: |
      choco install -y python
      choco install -y r
      choco install -y anaconda
      choco install -y vscode
      pip install numpy pandas scikit-learn matplotlib
